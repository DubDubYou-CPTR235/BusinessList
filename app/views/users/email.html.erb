<h1>User#email</h1>

<% if user_signed_in?() %>
	<h3>You are logged in!
	<%= link_to("Log Out", destroy_user_session_path, :method => :delete) %>
	</h3>
<% end %>
<p>Find me in app/views/user/email.html.erb</p>
